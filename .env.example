# This file is a reference only. Use the environment-specific files:
#
#   .env.hardhat  — local Hardhat dev   (committed, safe — uses public test keys)
#   .env.prod     — Sepolia production  (gitignored — contains real private keys)
#
# Both files are pre-created with instructions. Fill in the blanks and follow
# the usage comment at the top of each docker-compose file.
